# 每日清单生成采用「权重加权抽取 + 快照固化」

每日清单生成算法：取当月龄阶段活动池 → 按里程碑达成状态对能力领域加权（未达成 ×1.5 加重、已达成 ×0.6 减少）→ 按权重抽取 N 个活动（N 按月龄浮动：0-1月=3、2-6月=4、7-12月=5、13月+=6）→ 每个活动带固定每日目标次数 → 生成后固化为快照。

父母可替换当日清单中个别项目（从活动池选一顶替，被替换项当天消失，仅影响当日快照，次日重新生成）。里程碑累计次数达阈值时在清单页建议确认，确认后次日算法生效。

权重系数（1.5/0.6）为初始值，上线后可根据数据调整；算法本身保持"加权抽取"的稳定结构。
